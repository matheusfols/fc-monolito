const mockPaymentInputApproved = {
  orderId: "order-1",
  amount: 100,
};

const mockPaymentInputError = {
  orderId: "order-1",
  amount: -1,
};

const mockPaymentInputDeclined = {
  orderId: "order-1",
  amount: 50,
};

export { mockPaymentInputApproved, mockPaymentInputError, mockPaymentInputDeclined };
